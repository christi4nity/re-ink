<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Self-hosted sync servers may run on arbitrary LAN or Tailscale hosts.
         SyncClient validates which HTTP hosts are allowed. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
